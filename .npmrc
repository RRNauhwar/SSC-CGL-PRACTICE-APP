# Ensure deterministic, strict dependency resolution across the monorepo.
strict-peer-dependencies=false
auto-install-peers=true
# Hoisting pattern that keeps NestJS/Prisma tooling resolvable while staying isolated.
shamefully-hoist=false
# Fail installs if the lockfile is out of date (CI safety).
prefer-frozen-lockfile=true
engine-strict=true
